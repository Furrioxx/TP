<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3" id="nav">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">
			<img src="{{logo}}" alt="Logo" width="30" height="24">
		</a>
		{% if user %}
			<p>{{ user.name }}
				-
				{{ user.email }}</p>
		{% endif %}

		<a href="{{ path("logout") }}">
			<button class="btn btn-danger btn-sm" type="button">
				Se déconnecter
			</button>
		</a>
	</div>
</nav>
